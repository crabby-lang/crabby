// Crabby's Lambda operation

let x = lambda(a) {
    return a + 10
}

let square = |x| x * x
let double = lambda x: x * 2

print(x(5))
print(square(5))
print(double(10))

// Got any problem? You can create an issue for this file!