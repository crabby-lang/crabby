// Crabby Mapping function - EXPERIMENTAL

let numbers = [1, 2, 3, 4]
let squared = numbers.map(|x| x * x)

print(squared)

let doubled = numbers.map(lambda x: x * 2)
print(doubled)

// Got any problem? You can create an issue for this file!
