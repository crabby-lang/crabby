// Crabby's 'Struct' and 'Where' statements

struct Point where x >= 0 {
    x: i64,
    y: i64,
}

pub struct Person {
    name: String,
    age: i64,
    hobbies: String,
    birthday: Date,
}
