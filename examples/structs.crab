// Crabby's 'Struct' and 'Where' statements

struct Point where x >= 0 {
    x: Int,
    y: Int,
}

pub struct Person {
    name: String,
    age: Int,
    hobbies: String,
    birthday: Date,
}
